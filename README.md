# Homebrew Tap

Homebrew formulae and casks for [Warpforge](https://github.com/warpforgehq).

## Usage

```sh
brew install --cask warpforge
```

The `yett` formula appears here after the first `yett` release.
